services:
  max-forward-bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8091:8091"
    volumes:
      - .:/app  # Это сохраняет config.json даже после удаления контейнера
